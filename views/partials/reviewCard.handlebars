<div class="row">
    <div class="col s12 m6">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{ book.title }}</span>
          <p>{{ review }}</p>
        </div>
        <div class="card-action">
          <a href="#">This review provided by {{ user.name }}</a>
        </div>
      </div>
    </div>
  </div>